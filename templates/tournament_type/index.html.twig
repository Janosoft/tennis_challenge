{% extends 'base.html.twig' %}

{% block title %}Listado de Tipos de Torneo{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Listado de Tipos de Torneo</h1>
		<table class="table table-sm caption-top">
			<thead class="table-light">
				<tr>
					<th>ID</th>
					<th>Título</th>
					<th>Habilidades En Juego</th>
				</tr>
			</thead>
			<tbody>
				{% for tournament_type in tournament_types %}
					<tr>
						<td>{{tournament_type.getId()}}</td>
						<td><a href="/tournament_type/{{ tournament_type.getTitle()}}">{{tournament_type.getTitle()}}</a></td>
						<td>{{tournament_type.getSkills()|join(', ')}}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
